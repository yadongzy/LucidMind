# LucidMind 项目规则 v1.3

> **规则已迁移至 `.rules/` 文件夹。**
> 每条规则一个独立文件，便于管理和强制执行。
> **每次执行任务前，必须先读取 `.rules/` 文件夹中的所有规则。**

详见 [.rules/README.md](.rules/README.md)

---

以下为规则全文备份（权威版本在 `.rules/` 文件夹）：

---

## 第零条：项目存在的理由

```
LucidMind 做两件事：
1. 想尽一切方法完成任务（永不放弃）
2. 想尽一切办法让自己更聪明（持续进化）

用户能看到它的全部思考过程。这是它和其他 Agent 的唯一区别。
如果用户看不到思考过程 → 项目失去意义。
如果思考过程是假的 → 比没有更糟。
```

---

## 第一条：架构 — 六边形（Ports & Adapters）

```
Brain 只认识 Port（接口），不认识 Adapter（实现）。
换 Adapter 不改 Brain 一行代码。
新功能 = 新 Adapter，不是改核心。

六个 Port：
  LLM Port      — 与大模型对话
  Tool Port     — 执行工具
  Memory Port   — 记忆存取
  Stream Port   — 思维流输出
  Channel Port  — 接收用户输入
  Learning Port — 经验学习
```

---

## 第二条：构建纪律 — Walking Skeleton

```
铁律 1：一次一个模块，跑通再做下一个
铁律 2：跑通 = 浏览器真实操作验证，不是 pytest mock
铁律 3：Brain 只调 Port，永远不直接调 Adapter
铁律 4：每次修改后，已通过的真实测试不能退化
```

### 构建顺序（严格按序）

| 阶段 | 内容 | 验证标准 |
|------|------|---------|
| S0 | 项目骨架 + 规则 + 空端口 | pytest 能运行，0 errors |
| S1 | Brain + LLM Port + DeepSeek Adapter | 浏览器发"你好" → 收到回复 |
| S2 | Stream Port + WebSocket Adapter | 浏览器能看到思考过程 |
| S3 | Tool Port + Shell Adapter | 浏览器问系统信息 → 返回真实数据 |
| S4 | Tool: File Adapter | 创建文件 → 磁盘真实存在 |
| S5 | Tool: WebSearch Adapter | 问天气 → 真实搜索结果 |
| S6 | Memory Port + JSON Memory | 暗号测试 → 新对话仍记得 |
| S7 | Ralph 循环（永不放弃） | 多步任务 → 自动重试直到完成 |
| S8 | Learning Port + JSON Lessons | 纠正 → 下次行为改变 |
| S9 | 完整性验收 | S1-S8 全部重测通过 |

---

## 第三条：代码边界（硬性上限）

| 文件类型 | 行数上限 | 超出处理 |
|---------|---------|---------|
| brain.py | 500 行 | 拆分为 mixin 或委托给 Port |
| 每个 Port 接口 | 50 行 | 职责不单一，需拆分 |
| 每个 Adapter | 300 行 | 拆分为子模块 |
| api/main.py | 200 行 | 拆分为 routes/ |
| 单个测试文件 | 300 行 | 按功能拆分 |
| 前端 JS | 500 行 | 拆分为模块 |

---

## 第四条：测试标准

```
每个模块完成时必须提供：

1. 单元测试（pytest）：
   - ≥ 1 个正向用例
   - ≥ 1 个异常用例
   - 运行命令和结果记录在 PROGRESS.md

2. 真实操作测试（浏览器）：
   - 在前端页面执行
   - 记录：输入 → 思维流 → 回复 → 耗时 → 判定
   - 结果记录在 PROGRESS.md

3. 模块通过真实测试后标记 [LOCKED]
   - 修改锁定模块需声明原因并等待确认
```

---

## 第五条：锁定清单

```
[LOCKED] 文件列表（通过真实测试后加入）：

（尚无锁定文件 — S0 阶段）

修改流程：
Step 1: 声明要改什么、为什么
Step 2: 等待确认
Step 3: 修改
Step 4: 重跑所有真实测试
Step 5: 全部通过后重新锁定
```

---

## 第六条：扩展规则

```
添加新 Adapter（不需要确认）：
1. 在 adapters/ 对应目录下新建文件
2. 实现对应 Port 的接口
3. 在配置中注册
4. 通过真实测试
5. 不修改 brain.py 任何代码

添加新 Port（需要确认）：
1. 声明新 Port 的目的和接口
2. 等待确认
3. 创建接口文件 + 至少一个 Adapter
4. 通过真实测试
```

---

## 第七条：模块日志

```
每个模块一个独立日志文件，存放在 logs/ 目录下。
日志不是走形式——是给未来的自己和用户看的，必须看得懂。

日志文件命名：
  logs/brain.log          — Brain 核心流程
  logs/llm.log            — LLM 调用（请求、响应、耗时、错误）
  logs/tools.log          — 工具调用（参数、结果、耗时）
  logs/memory.log         — 记忆存取
  logs/stream.log         — 思维流事件
  logs/learning.log       — 经验学习
  logs/api.log            — API 请求

每条日志必须包含：
  时间 | 模块 | 动作 | 结果 | 详情（如有）

重点记录：
  - 每次 LLM 调用的输入摘要、输出摘要、耗时、token 用量
  - 每次工具调用的参数、返回结果、成功/失败
  - 每次错误的完整堆栈和根因分析
  - 每次反思：做了什么、结果如何、学到了什么

日志不能：
  - 记录完整的 API Key 或敏感信息
  - 只记「成功」不记过程
  - 用技术术语糊弄，要写人话
```

---

## 第八条：诚实铁律

```
诚实是本项目的第一位原则。以下行为绝对禁止：

1. 不可以凑合
   — 「差不多能用」不算完成。能用 ≠ 正确。
   — 每个模块必须真正跑通，不是看起来跑通。

2. 不可以「不影响就不改」
   — 发现问题必须记录，不管当前是否影响功能。
   — 隐患是定时炸弹，现在不炸不代表将来不炸。

3. 不可以「顺便就怎样」
   — 每个改动必须有明确目的。
   — 顺手优化 = 未经审视的风险。

4. 不可以「暂时没事」
   — 「暂时」是最危险的词。
   — 要么修好，要么写进待办并标注风险等级。

5. 不可以编造、蒙混过关
   — 不确定就说不确定。
   — 没测试就说没测试。
   — 不能用的功能不能说能用。
   — 引用代码必须先看原文，禁止凭记忆编造。
   — 测试结果必须是真实运行的，禁止捏造。

6. 不可以回避问题
   — 遇到难题不能绕着走。
   — 解决不了要诚实说明，而不是假装问题不存在。

违反以上任何一条 = 项目质量的根基被动摇。
发现违反时：立即停下 → 诚实记录 → 修正 → 继续。
```

---

## 第九条：对标竞争铁律

```
对标项目：OpenAkita（https://github.com/openakita/openakita）
本地参考：f:\verv\TransparentMind_v2.55_2026-02-21\openakita-main

原则：不是抄，是每个模块都比它强一点点。

每个模块完成时必须包含对标分析：

1. 找到 OpenAkita 对应功能
   — 它怎么做的？代码在哪？逻辑是什么？

2. 找出它的不足
   — 哪里可以更好？哪里有硬伤？哪里用户体验差？

3. LucidMind 必须在至少一个具体维度上超越它
   — 不是“感觉更好”，是可度量的：
     · 更快（耗时更短）
     · 更透明（思维流信息量更大）
     · 更简洁（代码行数更少）
     · 更可靠（错误处理更完善）
     · 更可扩展（接口更干净）

4. 记录在 PROGRESS.md 中
   — 格式：
     OpenAkita: [它怎么做的]
     不足: [哪里不好]
     LucidMind: [我们怎么做的]
     优势: [具体超越点 + 数据]

禁止：
  - “我们也有这个功能”不算超越，必须说明哪里更好
  - 没研究对标就说“我们更好” = 违反诚实铁律
  - 为了超越而过度复杂化 = 违反简洁原则
```

### 模块对标映射表

| LucidMind 阶段 | 对标 OpenAkita 组件 | 核心比较维度 |
|-----------------|------------------------|----------------|
| S1 LLM 调用 | Brain + LLMClient | 调用透明度、日志详细度 |
| S2 思维流 | （OpenAkita 无此功能） | 我们的独占优势 |
| S3 Shell 工具 | tools/shell.py | 安全性、日志、超时处理 |
| S4 文件工具 | tools/file.py | 原子写入、错误处理 |
| S5 搜索工具 | tools/web.py | 结果质量、失败处理 |
| S6 记忆 | Memory (Vector) | 简洁性、可靠性 |
| S7 Ralph 循环 | Ralph (Never-Give-Up) | 透明度、可控性 |
| S8 经验学习 | Evolution / SelfCheck | 可观察性、可验证性 |

---

## 版本历史

| 版本 | 日期 | 内容 |
|------|------|------|
| v1.0 | 2026-02-21 | 立项，从 TransparentMind 经验中诞生 |
| v1.1 | 2026-02-21 | 新增第七条（模块日志）、第八条（诚实铁律） |
| v1.2 | 2026-02-21 | 新增第九条（对标竞争铁律 — 每个模块比 OpenAkita 强一点点） |
| v1.3 | 2026-02-21 | 规则迁移至 .rules/ 文件夹，每条规则独立文件，强制任务前读取 |
