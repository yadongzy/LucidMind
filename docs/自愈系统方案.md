# LucidMind 自愈系统方案

**版本**: v1.0  
**日期**: 2026-03-01  
**状态**: 方案设计  
**基于**: 深度代码审计 + 学术文献研究

---

## 一、背景与问题

### 1.1 当前系统现状

LucidMind 已有一套诊断-修复闭环：

```
SelfCheckEngine.daily_check()  →  发现问题  →  report_issue()
                                                    │
RepairEngine.repair_all()  ←  get_open_issues()  ←──┘
       │
       ├─ _auto_fix_minor()   → 轻微修复
       ├─ _fix_medium()       → 中等修复
       ├─ _fix_severe()       → 严重修复
       └─ _fix_fatal()        → 致命问题
```

### 1.2 审计发现的问题

| # | 问题 | 严重度 | 文件 |
|---|------|--------|------|
| 1 | RepairEngine 去重逻辑指向 JSON 文件，系统已切到 SQLite | 致命 | repair_engine.py:63-73 |
| 2 | `_auto_fix_minor` 默认 `return True` 假阳性 | 严重 | repair_engine.py:76 |
| 3 | issue_tracker 无 max_retry 限制，导致 2476 次无效重试 | 严重 | issue_tracker.py:36-43 |
| 4 | `_fix_severe/_fix_fatal` 只打日志不修复 | 中等 | repair_engine.py:95-110 |
| 5 | Brain 已有 read_file/write_file/run_script/git_* 工具但从未用于自修复 | 架构缺陷 | — |

### 1.3 行业基准数据

| 来源 | 数据 |
|------|------|
| RepairAgent (ICSE 2025) | Defects4J 19.6% 自主修复率, $0.14/bug |
| SWE-bench Verified (2026) | 最强 Agent ≈ 62-65% 解决率 |
| SWE-bench Pro | 工业级 ≈ 43-46% |
| C3 AI Alchemist | 需沙箱 + CI/CD guardrails |

---

## 二、方案设计：「Brain 自服务 + 外部代理」双层架构

### 2.1 架构总览

```
┌──────────────────────────────────────────────────────┐
│                  SelfCheckEngine                      │
│          (daily_check + monthly_full_check)           │
│                                                      │
│  检查项: LLM可用性, 经验库完整性, 日志错误,             │
│         资源使用, 历史问题复查                          │
│                                                      │
│  发现问题 → report_issue(desc, severity)              │
└────────────────────┬─────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────┐
│              SmartRepairEngine                        │
│                                                      │
│  ┌────────────────────────────────────────────┐      │
│  │ L1: 数据层修复 (直接 API 调用)              │      │
│  │                                            │      │
│  │  经验库去重 → store.delete(duplicate_ids)   │      │
│  │  噪音清洗 → noise_filter + store.delete    │      │
│  │  过期清理 → curate() 已有                   │      │
│  │  配置重置 → 写默认配置文件                   │      │
│  │                                            │      │
│  │  无风险，直接执行                            │      │
│  └────────────────────────────────────────────┘      │
│                                                      │
│  ┌────────────────────────────────────────────┐      │
│  │ L2: Brain 自服务修复 (通过 Brain.process)   │      │
│  │                                            │      │
│  │  生成修复任务描述 →                         │      │
│  │    TaskDispatcher.enqueue() →               │      │
│  │    Brain.process() 使用已有工具:             │      │
│  │      1. read_file → 读取问题代码            │      │
│  │      2. LLM 分析 → 生成修复方案              │      │
│  │      3. git_status → 确认干净工作区          │      │
│  │      4. write_file → 写入修复               │      │
│  │      5. run_script → pytest 验证            │      │
│  │      6. git_commit → 提交修复               │      │
│  │      7. 失败 → git checkout 回滚            │      │
│  │                                            │      │
│  │  中等风险，需测试验证                        │      │
│  └────────────────────────────────────────────┘      │
│                                                      │
│  ┌────────────────────────────────────────────┐      │
│  │ L3: 外部代理修复 (通过 MCP/API)             │      │
│  │                                            │      │
│  │  Brain 生成:                               │      │
│  │    - 问题描述 (自然语言)                     │      │
│  │    - 相关代码片段                            │      │
│  │    - 错误日志                               │      │
│  │    - 期望行为                               │      │
│  │                                            │      │
│  │  发送渠道:                                  │      │
│  │    - MCP client → 外部 MCP server           │      │
│  │    - Claude Code headless CLI               │      │
│  │    - Windsurf CLI                           │      │
│  │    - Kiro CLI                               │      │
│  │                                            │      │
│  │  接收 patch → 验证 → 合并                   │      │
│  │  高风险，需人工确认                          │      │
│  └────────────────────────────────────────────┘      │
│                                                      │
│  安全机制:                                            │
│    ✦ max_retry=10 → 自动升级到下一层                  │
│    ✦ 核心文件保护名单 (brain.py, brain_daemon.py 等)   │
│    ✦ git branch 隔离修复                              │
│    ✦ pytest 必须通过才提交                             │
│    ✦ 修复前自动 git stash                              │
│    ✦ ToolSafetyGuard 审批                             │
└──────────────────────────────────────────────────────┘
```

### 2.2 修复层级与判定逻辑

```python
# 伪代码
async def smart_repair(issue):
    severity = issue["severity"]
    retries = issue["retries"]
    
    # L1: 数据层直接修复
    if severity == "minor" and retries < 3:
        return await self._data_layer_fix(issue)
    
    # L2: Brain 自服务修复
    if severity in ("minor", "medium") and retries < 10:
        return await self._brain_self_repair(issue)
    
    # L3: 外部代理修复
    if retries >= 10 or severity in ("severe", "fatal"):
        return await self._delegate_to_external(issue)
    
    # 超限: 自动关闭 + 通知用户
    if retries >= 20:
        close_issue(issue["id"], "auto_closed_max_retry")
        notify_user(f"问题无法自动修复: {issue['desc']}")
```

### 2.3 核心文件保护名单

以下文件 Brain **绝对不能修改**（修改可能导致系统崩溃）：

```python
_PROTECTED_FILES = [
    "brain.py",           # 核心推理循环
    "brain_daemon.py",    # 后台守护进程
    "brain_config.py",    # 配置
    "api/main.py",        # API 入口
    "api/startup.py",     # 启动流程
    "command_queue.py",   # 并发控制
    "ports/",             # 所有端口接口
]
```

### 2.4 Brain 自修复任务模板

```
你是 LucidMind 的自修复子系统。请按以下步骤修复问题：

问题描述: {issue_desc}
问题文件: {file_path}
问题行号: {line_range}

步骤：
1. 使用 read_file 读取问题文件
2. 分析问题根因
3. 使用 git_status 确认工作区干净
4. 使用 write_file 写入修复
5. 使用 run_script 执行: cd /path/to/project && python3 -m pytest tests/ -x -q
6. 如果测试通过 → 使用 git_commit 提交
7. 如果测试失败 → 使用 run_script 执行: cd /path/to/project && git checkout -- {file_path}

安全规则：
- 不得修改 _PROTECTED_FILES 中的文件
- 每次只修改一个文件
- 修改前必须先读取完整文件
- 修改后必须运行测试验证
```

---

## 三、联动设计

### 3.1 与经验库联动

```
修复成功 → _record_repair_lesson() → 记录到经验库
                                        │
下次类似问题 → get_lessons("修复") → 查找历史修复经验
                                        │
                                   优先尝试已验证的修复方法
```

### 3.2 与记忆文件联动

- 创建 `REPAIR_PATTERNS.md` evergreen 记忆文件
- 记录成功的修复模式（文件→问题→修复方法→验证结果）
- Brain 修复前先检索记忆文件

### 3.3 与学习引擎联动

- `LearningEngine.maybe_learn()` 分析 repair 失败的 issue
- 提取失败模式 → 生成"教训"经验
- 灵魂引擎可固化频繁出现的修复模式为规则

### 3.4 与 MCP 联动（外部代理）

```json
// data/mcp_servers.json 新增配置
{
  "name": "external_repair",
  "transport": "stdio",
  "command": "claude",
  "args": ["--model=opus", "--allowedTools=Bash,Read,Write,Edit",
           "--permission-mode=acceptEdits", "--output-format=stream-json"]
}
```

### 3.5 与前端联动

- 诊断页增加"修复状态"列：open → repairing → verifying → closed
- 增加"手动触发修复"按钮
- 增加"委托外部修复"按钮
- 修复进度实时推送到 WebSocket

---

## 四、实施路线

### Phase 0: 立即修复（今天）

| 任务 | 文件 | 改动量 |
|------|------|--------|
| 修复 RepairEngine JSON→SQLite | repair_engine.py | ~30行 |
| 修复假阳性 return True | repair_engine.py | 1行 |
| 增加 max_retry + auto_close | issue_tracker.py | ~15行 |
| 清除僵尸 issue | data/issues.json | 运行脚本 |

### Phase 1: L1 数据层修复（1-2天）

| 任务 | 说明 |
|------|------|
| RepairEngine 用 SQLite API 去重 | store.delete() |
| 增加噪音清洗修复 | 复用 noise_filter.py |
| 增加经验质量评分修复 | LLM 评分 + 低分删除 |

### Phase 2: L2 Brain 自服务修复（3-5天）

| 任务 | 说明 |
|------|------|
| 修复任务模板设计 | 自然语言 prompt |
| 核心文件保护名单 | _PROTECTED_FILES |
| git 安全回滚机制 | branch + checkout |
| pytest 验证集成 | run_script 调用 |
| 端到端测试 | 模拟修复场景 |

### Phase 3: L3 外部代理修复（探索）

| 任务 | 说明 |
|------|------|
| MCP 外部修复协议设计 | issue → patch 格式 |
| Claude Code headless 集成 | CLI 调用 |
| Windsurf/Kiro 集成探索 | API/CLI |
| 人工确认流程 | WebSocket 通知 + 确认 |

---

## 五、风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| Brain 修改代码导致系统崩溃 | 中 | 致命 | _PROTECTED_FILES + git 回滚 + pytest 验证 |
| LLM 生成错误修复 | 高 | 中 | 测试必须通过 + 只改一个文件 |
| 无限修复循环 | 中 | 低 | max_retry=20 自动关闭 |
| 外部 Agent 返回不兼容 patch | 中 | 中 | patch 格式验证 + 测试 |
| 修复引入新 bug | 中 | 高 | git branch 隔离 + 全量测试 |

---

## 六、成功指标

| 指标 | 当前 | Phase 0 后 | Phase 2 后 |
|------|------|-----------|-----------|
| 僵尸 issue 数 | 2 (retries>800) | 0 | 0 |
| 修复成功率 | ~0% (假阳性) | >80% (L1) | >60% (L2) |
| 平均修复时间 | ∞ (永不修复) | <1分钟 (L1) | <5分钟 (L2) |
| 人工干预率 | 100% | <20% | <10% |

---

## 七、自愈系统完整逻辑构建图（基于当前代码实况）

> 更新: 2026-03-01 18:00，基于 commit `60ae9e3` 后实际代码绘制

### 7.1 全局数据流

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          LucidMind 自愈系统完整架构                               │
│                                                                                 │
│  ┌──────────┐    用户消息     ┌──────────┐    OODA 循环    ┌──────────────────┐  │
│  │  用户/老师 │ ─────────────→ │ WebSocket │ ──────────────→ │   BrainDaemon    │  │
│  │  (输入源)  │               │ Channel   │                │  (brain_daemon   │  │
│  └──────────┘               └────┬─────┘                │   .py 206行)     │  │
│                                   │                       └────────┬─────────┘  │
│                                   │ 直连                           │              │
│                                   ▼                               │ 空闲时        │
│                          ┌──────────────┐                        ▼              │
│                          │  Brain.process │◄─── 任务出队 ◄── TaskDispatcher      │
│                          │  (brain.py     │                  (task_dispatcher    │
│                          │   451行)       │                   .py 300行)         │
│                          └──────┬───────┘                                       │
│                                  │                                               │
│          ┌───────────────────────┼───────────────────────┐                       │
│          ▼                       ▼                       ▼                       │
│  ┌──────────────┐  ┌──────────────────┐  ┌──────────────────────┐              │
│  │ BrainContext  │  │ BrainResilience  │  │   BrainLearning      │              │
│  │ Mixin (287行) │  │ Mixin (275行)    │  │   Mixin (258行)      │              │
│  │              │  │                  │  │                      │              │
│  │ 消息构建      │  │ LLM重试(Ralph)   │  │ 经验检测/学习         │              │
│  │ 流式输出      │  │ 工具重试          │  │ ACE反思               │              │
│  │ 身份加载      │  │ 自检诊断          │  │ effectiveness标记     │              │
│  └──────────────┘  │ 错误处理          │  └──────────────────────┘              │
│                     │                  │                                         │
│                     │ ┌──────────────┐ │                                         │
│                     │ │BrainCompact  │ │                                         │
│                     │ │Mixin (253行) │ │                                         │
│                     │ │              │ │                                         │
│                     │ │token估算     │ │                                         │
│                     │ │历史压缩      │ │                                         │
│                     │ │Memory Flush  │ │                                         │
│                     │ │消息清洗      │ │                                         │
│                     │ └──────────────┘ │                                         │
│                     └──────────────────┘                                         │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 自愈闭环核心流程

```
╔═══════════════════════════════════════════════════════════════════════════╗
║                    BrainDaemon._think_loop() OODA 主循环                 ║
║                                                                         ║
║  每轮: Observe → Orient → Decide → Act → Sleep(interval)               ║
║                                                                         ║
║  ┌─────────────────────────────────────────────────────────────────┐    ║
║  │ Observe (观察)                                                   │    ║
║  │                                                                  │    ║
║  │  td.release_stuck_tasks()  ← 释放卡住的 running 任务             │    ║
║  │  _observe()                ← DaemonObserveMixin 检测环境变化      │    ║
║  └──────────────────────────────┬──────────────────────────────────┘    ║
║                                  ▼                                      ║
║  ┌─────────────────────────────────────────────────────────────────┐    ║
║  │ Orient + Decide + Act (定向+决策+行动)                           │    ║
║  │                                                                  │    ║
║  │  有任务？ ──YES──→ td.dequeue() → _execute_task() (最多3个/轮)   │    ║
║  │     │                                                            │    ║
║  │     NO                                                           │    ║
║  │     │                                                            │    ║
║  │     ▼                                                            │    ║
║  │  _run_engines_inner() ← 通过 CRON Lane 隔离执行                  │    ║
║  │     │                                                            │    ║
║  │     ├─ 1. DailyRoutineEngine.run_daily_routine()                │    ║
║  │     │     └─ 5项必做: 总结→学习→规划→自检→复查                    │    ║
║  │     │                                                            │    ║
║  │     ├─ 2. SelfCheckEngine.daily_check()  ──────────────────┐    │    ║
║  │     │     └─ 6项检查:                                       │    │    ║
║  │     │        ① 健康自检(_self_diagnose)                      │    │    ║
║  │     │        ② LLM可用性                                    │    │    ║
║  │     │        ③ 经验库完整性(重复+噪音)                       │    │    ║
║  │     │        ④ 日志错误检查                                  │    │    ║
║  │     │        ⑤ 历史未解决问题复查                             │    │    ║
║  │     │        ⑥ verifying问题回检                              │    │    ║
║  │     │                                                       │    │    ║
║  │     │     severe/fatal → enqueue_self_check_issue() ────┐   │    │    ║
║  │     │                                                    │   │    │    ║
║  │     │                    ┌────────────────────────────────┘   │    │    ║
║  │     │                    ▼                                    │    │    ║
║  │     │              ┌──────────┐                              │    │    ║
║  │     │              │ issue    │ ◄── report_issue()           │    │    ║
║  │     │              │ tracker  │                              │    │    ║
║  │     │              │ (131行)  │     状态: open → verifying   │    │    ║
║  │     │              │          │            → closed          │    │    ║
║  │     │              │          │     max_retry=20 自动关闭     │    │    ║
║  │     │              └────┬─────┘                              │    │    ║
║  │     │                    │ get_open_issues()                  │    │    ║
║  │     │                    ▼                                    │    │    ║
║  │     ├─ 3. RepairEngine.repair_all()  ◄───────────────────────┘    │    ║
║  │     │     └─ 分级修复（详见 7.3）                                  │    ║
║  │     │                                                            │    ║
║  │     ├─ 4. LearningEngine.maybe_learn()                          │    ║
║  │     │     └─ 3:00-6:00 或 空闲≥5轮时:                            │    ║
║  │     │        ① 种子经验注入                                       │    ║
║  │     │        ② 分析难题                                           │    ║
║  │     │        ③ 处理老师教学                                       │    ║
║  │     │        ④ 复习失败经验                                       │    ║
║  │     │        ⑤ 灵魂进化(高频经验→固化为规则)                       │    ║
║  │     │                                                            │    ║
║  │     └─ 5. 记忆备份 + 向量回填                                     │    ║
║  │                                                                  │    ║
║  │  _teaching_cycle() ← 老师教学处理                                │    ║
║  │  _proactive_health_check() ← 每10轮主动健康检查                  │    ║
║  └─────────────────────────────────────────────────────────────────┘    ║
║                                                                         ║
║  Sleep: td.compute_interval() 或 _wake_event 唤醒                      ║
╚═══════════════════════════════════════════════════════════════════════════╝
```

### 7.3 RepairEngine 三层修复决策树

```
                        ┌──────────────┐
                        │ open issue   │
                        │ (发现问题)    │
                        └──────┬───────┘
                               │
                    ┌──────────┴──────────┐
                    │  severity 判断       │
                    └──┬────┬────┬────┬───┘
                       │    │    │    │
              minor    │    │    │    │ fatal
                ┌──────┘    │    │    └──────┐
                ▼      medium    severe       ▼
        ┌───────────┐   │    │         ┌───────────┐
        │ L1: 数据层 │   │    │         │ L3: 外部   │
        │ 直接修复   │   │    │         │ 代理修复   │
        │            │   │    │         │            │
        │ ·经验去重   │   │    │         │ ·MCP server│
        │ ·噪音清洗   │   │    │         │ ·Claude CLI│
        │ ·过期清理   │   │    │         │ ·人工介入   │
        └──────┬────┘   │    │         └──────┬────┘
               │        │    │                │
               │   ┌────┴────┴────┐           │
               │   │  retries<5   │           │
               │   │  → 等待重试   │           │
               │   │              │           │
               │   │  retries≥5   │           │
               │   │  → L2 升级   │           │
               │   │              │           │
               │   │  retries≥10  │           │
               │   │  → L3 升级   │           │
               │   └──────┬───────┘           │
               │          ▼                   │
               │  ┌───────────────┐           │
               │  │ L2: Brain     │           │
               │  │ 自服务修复     │           │
               │  │               │           │
               │  │ 生成修复prompt │           │
               │  │     ↓         │           │
               │  │ TaskDispatcher│           │
               │  │ .enqueue()    │           │
               │  │     ↓         │           │
               │  │ Brain.process │           │
               │  │ 使用已有工具:  │           │
               │  │  read_file    │           │
               │  │  write_file   │           │
               │  │  run_script   │           │
               │  │  git_commit   │           │
               │  │  git_checkout │           │
               │  └──────┬────────┘           │
               │         │                    │
               ▼         ▼                    ▼
        ┌─────────────────────────────────────────┐
        │           修复结果判定                     │
        │                                         │
        │  成功 → _verify_fix() 回检               │
        │    │                                    │
        │    ├─ 验证通过 → mark_verifying()        │
        │    │              → 连续2次无复发→closed  │
        │    │              → _record_repair_lesson │
        │    │                                    │
        │    └─ 验证失败 → bump_retry()            │
        │                                         │
        │  失败 → bump_retry()                     │
        │    │                                    │
        │    ├─ retries < max_retry → 下轮重试     │
        │    └─ retries ≥ 20 → 自动关闭(僵尸防护)  │
        └─────────────────────────────────────────┘
```

### 7.4 并发隔离架构 (CommandQueue 四车道)

```
┌─────────────────────────────────────────────────────┐
│              CommandQueue (command_queue.py)          │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌────────┐  ┌───────┐│
│  │ CHAT ×3  │  │ DAEMON ×2│  │ CRON ×1│  │SUB ×2 ││
│  │          │  │          │  │        │  │       ││
│  │ 用户对话  │  │ 普通任务  │  │ 后台引擎│  │ 子任务 ││
│  │ ←WS直连  │  │ ←td出队   │  │ ←自检   │  │ ←分解  ││
│  │          │  │          │  │ ←修复   │  │       ││
│  │          │  │          │  │ ←学习   │  │       ││
│  └──────────┘  └──────────┘  └────────┘  └───────┘│
│                                                     │
│  互不阻塞: 用户聊天不会被后台自检卡住                   │
│  优雅关闭: mark_gateway_draining + wait_for_idle      │
└─────────────────────────────────────────────────────┘
```

### 7.5 文件-模块对应表（当前代码实况）

```
自愈系统涉及文件:
                                                              行数    角色
┌─ brain_daemon.py ─────────────────────────────────────────── 206 ── 调度中枢 (OODA循环)
├─ brain_engines.py ────────────────────────────────────────── 296 ── 三引擎容器
│   ├─ SelfCheckEngine ── 每日/每月自检 (6项检查)
│   ├─ DailyRoutineEngine ── 每日5项必做
│   └─ LearningEngine ── 定时学习+灵魂进化
├─ repair_engine.py ────────────────────────────────────────── 348 ── 分级修复 (L1/L2/L3)
├─ issue_tracker.py ────────────────────────────────────────── 131 ── 问题追踪 (open→verifying→closed)
├─ task_dispatcher.py ──────────────────────────────────────── 300 ── 任务队列 (入队/出队/状态流转)
├─ task_dispatcher_utils.py ────────────────────────────────── ???  ── 队列工具函数
├─ brain_task_executor.py ──────────────────────────────────── 237 ── 任务执行 (while-true重试)
├─ command_queue.py ────────────────────────────────────────── 296 ── 四车道并发控制
├─ diagnostics.py ──────────────────────────────────────────── 157 ── 诊断事件收集
├─ brain_resilience.py ─────────────────────────────────────── 275 ── 韧性 (LLM重试/自检/错误)
│   └─ brain_compact.py ───────────────────────────────────── 253 ── 压缩/清洗
├─ brain_learning.py ───────────────────────────────────────── 258 ── 经验学习
├─ brain_context.py ────────────────────────────────────────── 287 ── 消息构建/流式
├─ identity/soul_engine.py ─────────────────────────────────── 206 ── 灵魂进化
├─ memory/noise_filter.py ──────────────────────────────────── ???  ── 噪音过滤
├─ teacher_channel.py ──────────────────────────────────────── ???  ── 教学通道 (求助老师)
└─ data/issues.json ────────────────────────────────────────── ---  ── 问题持久化存储
```

### 7.6 端到端场景示例

```
场景: "经验库有15条重复"

  时间线                 组件                     动作
  ─────                ─────                   ─────
  03:00   BrainDaemon._think_loop()        → 空闲，执行引擎
    │     DailyRoutineEngine               → 每日5项 ✅
    │     SelfCheckEngine.daily_check()     → 检查经验库
    │       └─ 发现 15条重复                → report_issue("经验库有15条重复", "minor")
    │                                      → data/issues.json 写入
    │
    │     RepairEngine.repair_all()         → 读取 open issues
    │       └─ severity=minor              → _auto_fix_minor()
    │          └─ "重复" in desc           → 去重逻辑
    │             ├─ get_lessons(limit=500)  → 获取所有经验
    │             ├─ 找到 15 个重复 trigger
    │             ├─ store.delete(dup_ids)   → SQLite 删除
    │             └─ return True
    │
    │     _verify_fix()                     → 再次检查重复数
    │       └─ 重复数=0                    → mark_verifying()
    │
  04:00   下一轮 daily_check()              → check_verifying_issues()
    │       └─ verify_count=1/2             → 继续观察
    │
  05:00   下一轮 daily_check()              → check_verifying_issues()
    │       └─ verify_count=2/2             → status="closed" ✅
    │       └─ _record_repair_lesson()      → 经验库记录修复方法
    ▼
  完成: 问题从发现到关闭，全自动，零人工干预
```

---

## 八、前端自愈可视化方案（2026-03-01）

### 8.1 问题诊断：数据流断裂

当前后端自愈闭环完整（SelfCheckEngine → issue_tracker → RepairEngine → verifying → closed），
但 `issue_tracker.py` 没有 REST API 层，前端完全无法访问 issue 数据。

| 后端模块 | 数据 | API 状态 |
|----------|------|----------|
| `diagnostics.py` (DiagnosticEvent) | 性能事件（工具延迟、API 请求等） | ✅ 4 个端点 |
| `issue_tracker.py` (Issue) | 自愈问题（open/verifying/closed） | ❌ **无 API** |
| `repair_engine.py` (RepairEngine) | 修复操作 | ❌ 无独立 API |

### 8.2 实施计划

| 阶段 | 任务 | 文件 | 行数 |
|------|------|------|------|
| **P0** | 修复 `iss.description` → `iss.desc` + 结构化渲染诊断结果 | `diagnostics.js` | ~15行改动 |
| **P1** | 新增 Issue CRUD API（list/repair/close） | `api/issues.py` (新) + `api/main.py` | ~60行新文件 |
| **P2** | 诊断页新增「🏥 自愈问题」Tab — issue 列表 + 状态徽章 + 操作按钮 | `diagnostics.js` | ~150行改动 |
| P3 | 独立「自愈中心」页面 | `views/healing.js` (新) | ~300行 |

### 8.3 Issue API 设计

```
GET  /api/issues              → 全部 issue (open+verifying+closed)
GET  /api/issues/open         → 仅 open
POST /api/issues/{id}/repair  → 手动触发单条修复 (调用 RepairEngine)
POST /api/issues/{id}/close   → 手动关闭 (误报/已自行解决)
```

### 8.4 前端自愈问题 Tab 设计

```
┌─────────────────────────────────────────────────────────┐
│  [摘要统计] [时间线] [事件列表] [实时日志] [🏥 自愈问题]  │
├─────────────────────────────────────────────────────────┤
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌────────┐             │
│  │ Open │  │验证中│  │已关闭│  │修复成功率│             │
│  │  1   │  │  0   │  │ 12   │  │ 83.3%  │             │
│  └──────┘  └──────┘  └──────┘  └────────┘             │
│                                                         │
│  [全部] [Open] [验证中] [已关闭]                         │
│                                                         │
│  ┌─ 🟡 minor ──────────────────────────────────────┐   │
│  │ 经验库有154条重复                                 │   │
│  │ 来源: daily_check · 发现: 18:11 · 重试: 3次      │   │
│  │ 状态: open                                       │   │
│  │                          [🔧 手动修复] [✕ 关闭]   │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 8.5 L3 委托外部修复使用方法

**通道 A — MCP 外部修复服务器**：
在 `config/mcp_servers.json` 添加 `external_repair` 服务器配置，
MCP server 需暴露包含 `repair`/`fix`/`diagnose` 关键词的工具。

**通道 B — CLI 代理**：
安装 `claude` CLI (`npm install -g @anthropic-ai/claude-code`)，
系统自动检测 PATH 中的 `claude` 命令，retries 达标后自动调用。

**升级条件**：medium retries≥10 / severe retries≥10 / fatal 直接 → L3
